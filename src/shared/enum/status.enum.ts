export enum EBookingStatusType {
  CONFIRMED = 'confirmed',
  CANCEL = 'cancel',
  VISITED = 'visited'
}
export enum EResturantStatusType {
  OPEN = 'open',
  CLOSED = 'closed'
}
export enum EBookingsType {
  CONFIRMED = 'confirmed',
  CANCELED = 'canceled'
}
